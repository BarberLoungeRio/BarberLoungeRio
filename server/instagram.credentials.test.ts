import { describe, it } from "vitest";

describe("Instagram Graph credentials", () => {
  it.skip("will validate the Meta token when the user re-enables the official feed integration", () => {
    // Intentionally skipped: the user requested continuing without the Facebook/Meta setup.
    // The public site uses the clickable Instagram profile fallback until that setup is resumed.
  });
});
